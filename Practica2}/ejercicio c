const personas = [
    { nombre: "Ana", edad: 22 },
    { nombre: "Luis", edad: 35 },
    { nombre: "María", edad: 28 }
];

const nombre = personas.find (personas =>
     personas.nombre == "Luis")
console.log (nombre);

personas.forEach (personas =>
    personas.nombre) 
console.log (personas);

const SumaEdades = personas.reduce ((acumulador, persona) => 
    acumulador + persona.edad, 0);
console.log (SumaEdades);